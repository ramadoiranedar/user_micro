package str

// config database
const (
	CONFIG_DB_USER = `db.user`
	CONFIG_DB_PASS = `db.pass`
	CONFIG_DB_HOST = `db.host`
	CONFIG_DB_PORT = `db.port`
	CONFIG_DB_NAME = `db.name`
)

// config app
const (
	CONFIG_APP_NAME          = `app.name`
	CONFIG_APP_ENVIRONMENT   = `app.env`
	CONFIG_APP_MAX_UPLOAD_MB = `app.max_upload_mb`
)

// config foo
const (
	CONFIG_EXAMPLE_SECRET = `example.secret`
)

// more config
// ...
